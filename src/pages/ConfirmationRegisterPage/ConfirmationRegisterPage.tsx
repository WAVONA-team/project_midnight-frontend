import React from 'react';

import { ConfirmationRegisterForm } from '@/modules/Authorization';

export const ConfirmationRegisterPage: React.FC = React.memo(() => {
  return <ConfirmationRegisterForm />;
});
