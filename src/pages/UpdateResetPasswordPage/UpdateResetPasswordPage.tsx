import React from 'react';

import { UpdateResetPasswordForm } from '@/modules/Authorization';

export const UpdateResetPasswordPage: React.FC = React.memo(() => {
  return <UpdateResetPasswordForm />;
});
